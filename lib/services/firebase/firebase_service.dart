import 'package:firebase_core/firebase_core.dart';

class FirebaseService {
  static Future<void> initialize() async {
    // Firebase.initializeAppは既にmain.dartで行われている前提
    // ここでは追加の初期化処理があれば実装
  }

  // 将来的に必要な共通メソッドを追加
}
